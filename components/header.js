import React from 'react';

export default () => {
    return (
        <nav className="navbar navbar-dark bg-dark">
            <a className="navbar-brand" href="#">Tweet Miner</a>
        </nav>
    );
};